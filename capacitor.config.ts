import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'EUPOP-Bot-Companion',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
